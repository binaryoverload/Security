name: Security
version: 1.7
main: es.esy.williamoldham.security.Main
permissions:
  security.bypass:
    description: Allows bypassing of the security
  security.notify:
    description: Will notify the user upon other players breaking the rule!
  security.*:
    description: Gives player permissions to all the commands in this plugin